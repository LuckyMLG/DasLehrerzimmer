<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<div class="container">
  <div class="nav">
  <% if (user) { %>
    Angemeldet als <%= user.username %>
    <% if (user.is_admin) { %> | <a href="/admin">Admin</a><% } %>
    | <a href="/teachers">Lehrer</a> | <a href="/logout">Logout</a>
  <% } else { %>
    <a href="/login">Login</a> | <a href="/register">Registrieren</a>
  <% } %>
  </div>
